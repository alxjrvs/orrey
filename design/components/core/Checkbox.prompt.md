One-line: opt-in flags in console forms, and the selection affordance on date-poll rows.

```jsx
<Checkbox checked={auto} onChange={setAuto} label="Auto-resolve this campaign's polls" />
```

Square, 14px, accent when checked. Labels are sentence case and state what being checked means, not what the toggle is.
