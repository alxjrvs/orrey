One-line: a closed list of console options — win rules, campaign kinds, location types.

```jsx
<Select options={['Game minimum','Quorum of roster','Best available','Organiser picks']} />
```

Wrap in `Field`. For more than about ten options, or anything the user might type, use an input with autocomplete instead.
